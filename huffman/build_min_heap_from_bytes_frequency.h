#ifndef BUILD_MIN_HEAP_FROM_BYTES_FREQUENCY_H
#define BUILD_MIN_HEAP_FROM_BYTES_FREQUENCY_H

#include "min_heap.h"

/*
It takes an array having the frequency of bytes
and enqueue them on a min heap.
*/
MinHeap *buildMinHeapFromBytesFrequency(size_t *bytesFrequency);

#endif
